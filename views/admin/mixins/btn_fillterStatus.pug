mixin btn_fillterStatus(fillterStatus)
    each item in fillterStatus
        button(
            class=`btn btn-sm me-2 btn-outline-success ${item.class}`
            button-status=item.status
        ) #{item.name}